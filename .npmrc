# Force npm to rebuild native addons on install
force=true

# Ensure platform-specific modules are correctly installed
unsafe-perm=true

# Use more reliable network settings
network-timeout=100000
fetch-retries=3
fetch-retry-factor=2 